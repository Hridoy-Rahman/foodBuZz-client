import React, { useState } from 'react';

const Recipies = () => {
    const [recipies,setRecipies]=useState([]);
    
    return (
        <div>
            
        </div>
    );
};

export default Recipies;